include ../config/menu
.menu
    .menu__body
        .menu__logo
            img(src="", alt="dinos-logo")
        nav.menu__nav
            each item in menu
                a(href=item.link).menu__item #{item.name}